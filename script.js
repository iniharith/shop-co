const https = require('https'); https.get('https://dribbble.com/shots/6298759-AI-technology-product-loading-animation-3', { headers: { 'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36' } }, (res) => { let data = ''; res.on('data', d => data += d); res.on('end', () => { const match = data.match(/<meta property="og:image" content="([^"]+)"/); console.log(match ? match[1] : 'Not found'); }); });
