"use client";

import Link from "next/link";
import { useEffect, useMemo, useRef, useState } from "react";
import { ArrowLeft, Download, ImagePlus, LayoutTemplate, Save, ShoppingBag, Sparkles, Sticker, Trash2, Upload, WandSparkles } from "lucide-react";

type BookSize = "A5" | "A6";
type PageChoice = "80" | "custom";
type Spread = { id: number; middleImage?: string; image?: string; caption: string; stickers?: string[] };
type ImageAdjust = { scale: number; x: number; y: number };
type AdjustmentHistory = { coverAdjust: ImageAdjust; imageAdjustments: Record<number, ImageAdjust> };

const makeSpreads = (count: number, existing: Spread[] = []): Spread[] => Array.from({ length: count }, (_, index) => ({ ...(existing[index] || {}), id: index + 1, caption: index === 0 ? "Cover" : `Page ${index}` }));
const sizeCopy: Record<BookSize, { label: string; dimensions: string; price: number; template: string; preview: string; innerPreview: string }> = {
  A5: { label: "A5 portrait", dimensions: "148 × 210 mm", price: 49, template: "INLAY PHOTOBOOK BINDER 1P.indd", preview: "/templates/photobook/a5-preview.png", innerPreview: "/templates/photobook/inlay/a5.jpg" },
  A6: { label: "A6 portrait", dimensions: "105 × 148 mm", price: 39, template: "INLAY PHOTOBOOK BINDER 1P A6.indd", preview: "/templates/photobook/a6-preview.png", innerPreview: "/templates/photobook/inlay/a6.jpg" },
};
const stickerChoices = ["★", "♥", "✦", "☀", "✿", "●"];
const coverDesigns: Record<BookSize, string[]> = { A5: ["D1", "D2", "D3", "D4", "D5", "D6", "D7", "D8", "D9", "D10"], A6: ["D1", "D2", "D5", "D7", "D8", "D10"] };

export default function DiyPhotobookPage() {
  const [bookSize, setBookSize] = useState<BookSize>("A5");
  const [pageChoice, setPageChoice] = useState<PageChoice>("80");
  const [customPages, setCustomPages] = useState("80");
  const [title, setTitle] = useState("Our little moments");
  const [subtitle, setSubtitle] = useState("A book made by you");
  const [coverDesign, setCoverDesign] = useState("D1");
  const [backgroundChoice, setBackgroundChoice] = useState("teal");
  const [colourChoice, setColourChoice] = useState("cream");
  const [spreads, setSpreads] = useState<Spread[]>(() => makeSpreads(80));
  const [coverImage, setCoverImage] = useState<string>();
  const [coverAdjust, setCoverAdjust] = useState<ImageAdjust>({ scale: 1, x: 0, y: 0 });
  const [imageAdjustments, setImageAdjustments] = useState<Record<number, ImageAdjust>>({});
  const [coverStickers, setCoverStickers] = useState<string[]>([]);
  const [showCover, setShowCover] = useState(true);
  const [selectedSpread, setSelectedSpread] = useState(0);
  const [saved, setSaved] = useState(false);
  const [templateReady, setTemplateReady] = useState(true);
  const [undoStack, setUndoStack] = useState<AdjustmentHistory[]>([]);
  const [redoStack, setRedoStack] = useState<AdjustmentHistory[]>([]);
  const dragOrigin = useRef<{ startX: number; startY: number; baseX: number; baseY: number }>();
  const pageCount = pageChoice === "80" ? 80 : Math.max(1, Math.min(300, Number(customPages) || 1));
  const currentSpread = spreads[selectedSpread] || spreads[0];
  const activeImage = showCover ? coverImage : (currentSpread?.middleImage || currentSpread?.image);
  const activeStickers = showCover ? coverStickers : (currentSpread?.stickers || []);
  const activeAdjust = showCover ? coverAdjust : (imageAdjustments[selectedSpread] || { scale: 1, x: 0, y: 0 });
  const total = useMemo(() => sizeCopy[bookSize].price + Math.max(0, pageCount - 80) * 2, [bookSize, pageCount]);

  useEffect(() => {
    const draft = window.localStorage.getItem("kampungcetak-photobook-draft");
    if (!draft) return;
    try {
      const parsed = JSON.parse(draft);
      const restoredPages = Number(parsed.pageCount) || 80;
      setBookSize(parsed.bookSize || "A5"); setPageChoice(restoredPages === 80 ? "80" : "custom"); setCustomPages(String(restoredPages));
      setTitle(parsed.title || "Our little moments"); setSubtitle(parsed.subtitle || "A book made by you"); setBackgroundChoice(parsed.backgroundChoice || "teal"); setColourChoice(parsed.colourChoice || "cream"); setCoverDesign(parsed.coverDesign || "D1"); setCoverImage(parsed.coverImage); setCoverAdjust(parsed.coverAdjust || { scale: 1, x: 0, y: 0 }); setCoverStickers(parsed.coverStickers || []); setImageAdjustments(parsed.imageAdjustments || {}); setSpreads(makeSpreads(restoredPages, parsed.spreads || []));
    } catch { window.localStorage.removeItem("kampungcetak-photobook-draft"); }
  }, []);
  useEffect(() => { setSpreads((items) => makeSpreads(pageCount, items)); setSelectedSpread((index) => Math.min(index, pageCount - 1)); }, [pageCount]);
  useEffect(() => { setTemplateReady(true); }, [bookSize]);

  const updateSpreadImage = (file?: File) => { if (!file || !file.type.startsWith("image/")) return; const image = URL.createObjectURL(file); if (showCover) setCoverImage(image); else setSpreads((items) => items.map((spread, index) => index === selectedSpread ? { ...spread, middleImage: image, image } : spread)); };
  const updateImageAdjust = (changes: Partial<ImageAdjust>, record = true) => { if (record) { setUndoStack((items) => [...items, { coverAdjust, imageAdjustments }]); setRedoStack([]); } if (showCover) setCoverAdjust((value) => ({ ...value, ...changes })); else setImageAdjustments((values) => ({ ...values, [selectedSpread]: { ...(values[selectedSpread] || { scale: 1, x: 0, y: 0 }), ...changes } })); };
  const undo = () => { setUndoStack((items) => { const previous = items.at(-1); if (!previous) return items; setRedoStack((future) => [...future, { coverAdjust, imageAdjustments }]); setCoverAdjust(previous.coverAdjust); setImageAdjustments(previous.imageAdjustments); return items.slice(0, -1); }); };
  const redo = () => { setRedoStack((items) => { const next = items.at(-1); if (!next) return items; setUndoStack((past) => [...past, { coverAdjust, imageAdjustments }]); setCoverAdjust(next.coverAdjust); setImageAdjustments(next.imageAdjustments); return items.slice(0, -1); }); };
  const beginImageDrag = (event: React.PointerEvent<HTMLDivElement>) => { if (!activeImage) return; event.currentTarget.setPointerCapture(event.pointerId); setUndoStack((items) => [...items, { coverAdjust, imageAdjustments }]); setRedoStack([]); dragOrigin.current = { startX: event.clientX, startY: event.clientY, baseX: activeAdjust.x, baseY: activeAdjust.y }; };
  const dragImage = (event: React.PointerEvent<HTMLDivElement>) => { if (!dragOrigin.current) return; updateImageAdjust({ x: Math.max(-45, Math.min(45, dragOrigin.current.baseX + (event.clientX - dragOrigin.current.startX) / 3)), y: Math.max(-45, Math.min(45, dragOrigin.current.baseY + (event.clientY - dragOrigin.current.startY) / 3)) }, false); };
  const endImageDrag = () => { dragOrigin.current = undefined; };
  const addSticker = (sticker: string) => { if (showCover) setCoverStickers((items) => [...items, sticker]); else setSpreads((items) => items.map((spread, index) => index === selectedSpread ? { ...spread, stickers: [...(spread.stickers || []), sticker] } : spread)); };
  const removeSticker = (stickerIndex: number) => { if (showCover) setCoverStickers((items) => items.filter((_, index) => index !== stickerIndex)); else setSpreads((items) => items.map((spread, index) => index === selectedSpread ? { ...spread, stickers: (spread.stickers || []).filter((_, itemIndex) => itemIndex !== stickerIndex) } : spread)); };
  const activeDesign = coverDesigns[bookSize].includes(coverDesign) ? coverDesign : coverDesigns[bookSize][0];
  const templatePreview = `/templates/photobook/covers/${bookSize.toLowerCase()}/${activeDesign.toLowerCase()}.png`;
  const previewImage = showCover ? templatePreview : sizeCopy[bookSize].innerPreview;
  const previewAspect = showCover ? "1190.55 / 841.89" : "936 / 1368";
  const draftData = { bookSize, pageCount, coverFinish: "Hardcover", coverDesign: activeDesign, coverImage, coverAdjust, coverStickers, imageAdjustments, title, subtitle, backgroundChoice, colourChoice, spreads };
  const saveDraft = () => { window.localStorage.setItem("kampungcetak-photobook-draft", JSON.stringify(draftData)); setSaved(true); window.setTimeout(() => setSaved(false), 2200); };
  const downloadDraft = () => { const url = URL.createObjectURL(new Blob([JSON.stringify(draftData, null, 2)], { type: "application/json" })); const anchor = document.createElement("a"); anchor.href = url; anchor.download = `kampungcetak-${bookSize.toLowerCase()}-photobook-draft.json`; anchor.click(); URL.revokeObjectURL(url); };
  const previewBackground = backgroundChoice === "sand" ? "linear-gradient(145deg,#d8b98c,#8b6b49)" : "linear-gradient(145deg,#087f73,#064e49)";
  const previewColour = colourChoice === "coral" ? "#f47c68" : "#f8e4a8";

  return (
    <main className="min-h-screen bg-background text-foreground">
      <header className="border-b border-border bg-card/90 px-4 py-4 backdrop-blur sm:px-8"><div className="mx-auto flex max-w-[1500px] items-center justify-between gap-4"><div className="flex items-center gap-3"><Link href="/" aria-label="Back to Kampung Cetak" className="rounded-full border border-border p-2 transition hover:border-primary hover:text-primary"><ArrowLeft className="size-4" /></Link><div><p className="text-xs font-bold uppercase tracking-[0.22em] text-primary">Kampung Cetak</p><h1 className="text-2xl font-bold tracking-tight sm:text-3xl">DIY Photobook</h1></div></div><div className="hidden items-center gap-2 text-sm text-muted-foreground md:flex"><Sparkles className="size-4 text-primary" /> Make it yours, page by page</div></div></header>
      <div className="mx-auto grid max-w-[1500px] gap-5 p-4 sm:p-6 lg:grid-cols-[280px_minmax(0,1fr)_320px]">
        <aside className="order-2 space-y-4 lg:order-1"><section className="rounded-2xl border border-border bg-card p-4 shadow-sm"><div className="mb-4 flex items-center gap-2"><LayoutTemplate className="size-4 text-primary" /><h2 className="font-semibold">Book setup</h2></div><label className="mb-2 block text-xs font-bold uppercase tracking-wider text-muted-foreground">Size</label><div className="grid grid-cols-2 gap-2">{(["A5", "A6"] as BookSize[]).map((size) => <button key={size} onClick={() => setBookSize(size)} className={`rounded-xl border p-3 text-left transition ${bookSize === size ? "border-primary bg-primary/10 ring-2 ring-primary/20" : "border-border hover:border-primary/50"}`}><span className="block font-bold">{size}</span><span className="text-xs text-muted-foreground">{sizeCopy[size].dimensions}</span></button>)}</div><label className="mb-2 mt-5 block text-xs font-bold uppercase tracking-wider text-muted-foreground" htmlFor="pages">Pages</label><select id="pages" value={pageChoice} onChange={(event) => setPageChoice(event.target.value as PageChoice)} className="w-full rounded-xl border border-input bg-background px-3 py-3 text-sm"><option value="80">80 pages</option><option value="custom">Custom</option></select>{pageChoice === "custom" && <input aria-label="Custom page count" type="number" min={1} max={300} value={customPages} onChange={(event) => setCustomPages(event.target.value)} className="mt-2 w-full rounded-xl border border-input bg-background px-3 py-3 text-sm" placeholder="Type page count" />}<div className="mt-4 rounded-xl border border-primary/20 bg-primary/5 p-3 text-sm"><span className="font-semibold">Cover finish:</span> Hardcover</div><div className="mt-4 rounded-xl bg-muted/60 p-3 text-xs leading-5 text-muted-foreground"><span className="font-semibold text-foreground">Active template:</span><br />{sizeCopy[bookSize].template}<br /><span className="text-[11px]">No suffix = A5 · “A6” suffix = A6</span></div></section><section className="rounded-2xl border border-border bg-card p-4 shadow-sm"><h2 className="mb-3 font-semibold">Your pages · cover + {pageCount} pages</h2><div className="grid max-h-80 grid-cols-4 gap-2 overflow-y-auto pr-1"><button onClick={() => setShowCover(true)} className={`relative aspect-[3/4] overflow-hidden rounded-lg border text-left ${showCover ? "border-primary ring-2 ring-primary/30" : "border-border"}`}><img src={previewImage} alt={`${bookSize} cover`} className="h-full w-full object-cover" /><span className="absolute bottom-0 left-0 right-0 bg-black/55 px-1 py-0.5 text-center text-[9px] text-white">Cover</span></button>{spreads.map((spread, index) => <button key={spread.id} onClick={() => { setShowCover(false); setSelectedSpread(index); }} className={`relative aspect-[3/4] overflow-hidden rounded-lg border text-left ${showCover ? "border-border" : selectedSpread === index ? "border-primary ring-2 ring-primary/30" : "border-border"}`}>{(spread.middleImage || spread.image) ? <img src={spread.middleImage || spread.image} alt="" className="h-full w-full object-cover" /> : <span className="grid h-full place-items-center bg-muted text-[10px] text-muted-foreground">{spread.caption}</span>}<span className="absolute bottom-0 left-0 right-0 bg-black/55 px-1 py-0.5 text-center text-[9px] text-white">{spread.id}</span></button>)}</div></section></aside>
        <section className="order-1 flex min-h-[620px] flex-col rounded-3xl border border-border bg-muted/40 p-4 shadow-inner sm:p-6 lg:order-2"><div className="mb-4 flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wider text-primary">Live preview · {sizeCopy[bookSize].label}</p><p className="text-sm text-muted-foreground">{pageCount} pages · {showCover ? "Cover" : `Page ${selectedSpread + 1}`}</p></div><span className="rounded-full bg-primary/10 px-3 py-1 text-xs font-semibold text-primary">Template preview</span></div><div className="mb-3 flex flex-wrap items-center justify-center gap-2 rounded-2xl border border-border bg-card/95 p-2 text-xs shadow-lg backdrop-blur"><button onClick={undo} disabled={!undoStack.length} className="rounded-lg border border-border px-3 py-2 font-semibold disabled:opacity-40">Undo</button><button onClick={redo} disabled={!redoStack.length} className="rounded-lg border border-border px-3 py-2 font-semibold disabled:opacity-40">Redo</button><label className="flex items-center gap-2 px-2 font-semibold">Size <input type="range" min="0.5" max="2.5" step="0.05" value={activeAdjust.scale} onChange={(event) => updateImageAdjust({ scale: Number(event.target.value) })} className="w-24" /></label><button onClick={() => updateImageAdjust({ x: -20 })} className="rounded-lg border border-border px-2 py-2">Left</button><button onClick={() => updateImageAdjust({ x: 0 })} className="rounded-lg border border-border px-2 py-2">Center</button><button onClick={() => updateImageAdjust({ x: 20 })} className="rounded-lg border border-border px-2 py-2">Right</button><span className="text-muted-foreground">Drag photo inside the box</span></div><div className="flex flex-1 items-center justify-center py-8"><div className="relative w-[min(76vw,520px)] overflow-hidden rounded-[1.25rem] border-[10px] border-white bg-white shadow-2xl" style={{ background: previewBackground, aspectRatio: previewAspect }}><img src={previewImage} alt={showCover ? `${bookSize} ${activeDesign} cover design` : `${bookSize} inlay template`} className="absolute inset-0 h-full w-full object-cover" onError={(event) => { event.currentTarget.style.display = "none"; setTemplateReady(false); }} />{!templateReady && <div className="absolute inset-0 grid place-items-center p-6 text-center text-sm font-semibold text-white">Template {activeDesign} preview is not available<br /><code>/public/templates/photobook/{bookSize.toLowerCase()}-preview.png</code></div>}{activeImage && <div className={`absolute z-10 cursor-move overflow-hidden ${showCover ? "left-[4%] top-[4%] h-[80%] w-[44%]" : "left-[7%] top-[5%] h-[66%] w-[62%]"} bg-black`} onPointerDown={beginImageDrag} onPointerMove={dragImage} onPointerUp={endImageDrag} onPointerCancel={endImageDrag}><img src={activeImage} alt="Image inside selected cover design" className="h-full w-full object-contain transition-transform" style={{ transform: `translate(${activeAdjust.x}%, ${activeAdjust.y}%) scale(${activeAdjust.scale})` }} /></div>}{activeStickers.map((sticker, index) => <button key={`${sticker}-${index}`} onClick={() => removeSticker(index)} className="absolute text-3xl drop-shadow" style={{ left: `${18 + (index % 3) * 27}%`, top: `${12 + Math.floor(index / 3) * 16}%`, color: previewColour }} aria-label="Remove sticker">{sticker}</button>)}</div></div><div className="flex items-center justify-center gap-3"><button disabled={showCover} onClick={() => showCover ? null : selectedSpread === 0 ? setShowCover(true) : setSelectedSpread((value) => Math.max(0, value - 1))} className="rounded-full border border-border bg-card px-4 py-2 text-sm disabled:opacity-40">Previous</button><span className="text-sm font-semibold text-muted-foreground">{showCover ? "Cover" : `${selectedSpread + 1} / ${spreads.length}`}</span><button disabled={!showCover && selectedSpread === spreads.length - 1} onClick={() => showCover ? setShowCover(false) : setSelectedSpread((value) => Math.min(spreads.length - 1, value + 1))} className="rounded-full border border-border bg-card px-4 py-2 text-sm disabled:opacity-40">Next</button></div></section>
        <aside className="order-3 space-y-4"><section className="rounded-2xl border border-border bg-card p-4 shadow-sm"><div className="mb-4 flex items-center gap-2"><WandSparkles className="size-4 text-primary" /><h2 className="font-semibold">Cover details</h2></div><label className="mb-1 block text-xs font-bold uppercase tracking-wider text-muted-foreground" htmlFor="title">Title</label><input id="title" value={title} onChange={(event) => setTitle(event.target.value)} className="mb-3 w-full rounded-xl border border-input bg-background px-3 py-3 text-sm" /><label className="mb-1 block text-xs font-bold uppercase tracking-wider text-muted-foreground" htmlFor="subtitle">Subtitle</label><input id="subtitle" value={subtitle} onChange={(event) => setSubtitle(event.target.value)} className="mb-4 w-full rounded-xl border border-input bg-background px-3 py-3 text-sm" /><div className="mt-4 border-t border-border pt-4"><label className="block text-xs font-bold uppercase tracking-wider text-muted-foreground" htmlFor="cover-design">Existing cover design</label><select id="cover-design" value={activeDesign} onChange={(event) => setCoverDesign(event.target.value)} className="mt-1 w-full rounded-xl border border-input bg-background px-3 py-3 text-sm">{coverDesigns[bookSize].map((design) => <option key={design} value={design}>{design}</option>)}</select><p className="mt-1 text-xs text-muted-foreground">Choose a design from the supplied {bookSize} cover templates.</p></div><div className="space-y-3 border-t border-border pt-4"><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Background / colours</p><label className="block text-sm font-semibold">Background<select value={backgroundChoice} onChange={(event) => setBackgroundChoice(event.target.value)} className="mt-1 w-full rounded-xl border border-input bg-background px-3 py-3 text-sm"><option value="teal">Teal</option><option value="sand">Sand</option></select></label><label className="block text-sm font-semibold">Colour<select value={colourChoice} onChange={(event) => setColourChoice(event.target.value)} className="mt-1 w-full rounded-xl border border-input bg-background px-3 py-3 text-sm"><option value="cream">Cream</option><option value="coral">Coral</option></select></label></div><div className="space-y-3 border-t border-border pt-4"><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Design image · {showCover ? "cover" : `page ${selectedSpread + 1}`}</p><div className="rounded-xl border border-border p-3"><div className="mb-2 flex items-center justify-between gap-2"><span className="text-sm font-semibold">Image inside design</span>{activeImage && <button onClick={() => showCover ? setCoverImage(undefined) : setSpreads((items) => items.map((spread, index) => index === selectedSpread ? { ...spread, middleImage: undefined, image: undefined } : spread))} className="text-xs font-semibold text-destructive">Clear</button>}</div><label className="flex cursor-pointer items-center justify-center gap-2 rounded-lg border border-dashed border-primary/40 px-3 py-3 text-xs font-semibold text-primary hover:bg-primary/5"><Upload className="size-3.5" /> Replace design image<input type="file" accept="image/*" className="sr-only" onChange={(event) => updateSpreadImage(event.target.files?.[0])} /></label></div></div><div className="space-y-2 border-t border-border pt-4"><p className="text-xs font-bold uppercase tracking-wider text-muted-foreground">Stickers · {showCover ? "cover" : `page ${selectedSpread + 1}`}</p><div className="flex flex-wrap gap-2">{stickerChoices.map((sticker) => <button key={sticker} onClick={() => addSticker(sticker)} className="grid size-10 place-items-center rounded-xl border border-border bg-background text-xl hover:border-primary" aria-label={`Add ${sticker} sticker`}>{sticker}</button>)}</div></div></section><section className="rounded-2xl border border-primary/20 bg-primary/5 p-4"><div className="flex items-end justify-between"><div><p className="text-sm text-muted-foreground">Estimated starting price</p><p className="text-3xl font-bold text-primary">RM {total.toFixed(2)}</p></div><span className="rounded-full bg-card px-3 py-1 text-xs font-bold">{bookSize}</span></div><p className="mt-3 text-xs leading-5 text-muted-foreground">Final delivery charges and production confirmation appear at checkout.</p></section><div className="grid gap-2"><button onClick={saveDraft} className="inline-flex items-center justify-center gap-2 rounded-full bg-neutral-950 px-5 py-3.5 font-bold text-white transition hover:bg-neutral-800"><Save className="size-4" /> {saved ? "Draft saved" : "Save draft"}</button><button onClick={downloadDraft} className="inline-flex items-center justify-center gap-2 rounded-full border border-border bg-card px-5 py-3.5 text-sm font-semibold hover:border-primary hover:text-primary"><Download className="size-4" /> Download draft data</button><Link href="/home/shop?category=photobook" className="inline-flex items-center justify-center gap-2 rounded-full border border-primary px-5 py-3.5 text-sm font-bold text-primary hover:bg-primary/5"><ShoppingBag className="size-4" /> Continue to photobook product</Link></div><div className="rounded-2xl border border-dashed border-border p-4 text-sm text-muted-foreground"><div className="mb-2 flex items-center gap-2 font-semibold text-foreground"><ImagePlus className="size-4 text-primary" /> Replace image inside design</div>Upload a photo to replace the image area inside the selected cover design.</div></aside>
      </div><footer className="mx-auto flex max-w-[1500px] items-center gap-2 px-4 pb-6 text-xs text-muted-foreground sm:px-6"><Trash2 className="size-3.5" /> A5/A6 template preview assets are required in the public templates folder.</footer>
    </main>
  );
}
