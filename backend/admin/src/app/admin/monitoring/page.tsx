"use client";

import { useEffect, useMemo, useState, type ReactNode } from "react";
import { Activity, AlertTriangle, CheckCircle2, Clock3, Database, Gauge, Globe2, HardDrive, Monitor, RefreshCw, Server, Wifi, XCircle, Zap } from "lucide-react";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import type { OpsStatus } from "@/api/ops";
import type { WebVitalsDays, WebVitalsSummaryItem } from "@/api/webVitals";
import PageContainer from "@/components/layout/page-container";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Skeleton } from "@/components/ui/skeleton";
import { useOpsOverview } from "@/hooks/useOpsOverview";
import { useWebVitalsStats } from "@/hooks/useWebVitals";

const dayOptions: WebVitalsDays[] = [7, 14, 30, 60, 90];
const metrics: Record<string, { label: string; good: number; poor: number }> = {
  lcp: { label: "LCP", good: 2500, poor: 4000 }, inp: { label: "INP", good: 200, poor: 500 },
  cls: { label: "CLS", good: 0.1, poor: 0.25 }, fcp: { label: "FCP", good: 1800, poor: 3000 }, ttfb: { label: "TTFB", good: 800, poor: 1800 },
};

const formatBytes = (value = 0) => {
  if (!value) return "0 B";
  const units = ["B", "KB", "MB", "GB", "TB"];
  const index = Math.min(Math.floor(Math.log(value) / Math.log(1024)), units.length - 1);
  return `${(value / 1024 ** index).toFixed(index ? 1 : 0)} ${units[index]}`;
};
const formatUptime = (seconds = 0) => `${Math.floor(seconds / 86400)}d ${Math.floor(seconds / 3600) % 24}h ${Math.floor(seconds / 60) % 60}m`;
const statusLabel = (state: OpsStatus["state"]) => state === "not_configured" ? "Not configured" : state.replace("_", " ");
const statusClass = (state: OpsStatus["state"]) => ({ healthy: "text-emerald-600 dark:text-emerald-300", degraded: "text-amber-600 dark:text-amber-300", down: "text-red-600 dark:text-red-300", stale: "text-amber-600 dark:text-amber-300", not_configured: "text-muted-foreground" }[state]);
const statusIcon = (state: OpsStatus["state"]) => state === "healthy" ? CheckCircle2 : state === "not_configured" ? Clock3 : state === "down" ? XCircle : AlertTriangle;
const ratingFor = (metric: string, value: number | null) => { const config = metrics[metric]; if (value === null || !config) return "none"; return value <= config.good ? "good" : value <= config.poor ? "needs improvement" : "poor"; };
const formatMetric = (metric: string, value: number | null) => value === null ? "—" : metric === "cls" ? value.toFixed(3) : `${Math.round(value)} ms`;

function GlassCard({ className = "", children }: { className?: string; children: ReactNode }) {
  return <Card className={`overflow-hidden border-border/70 bg-card/55 shadow-sm backdrop-blur-md ${className}`}>{children}</Card>;
}

function DependencyCard({ name, status, icon: Icon }: { name: string; status?: OpsStatus; icon: React.ComponentType<{ className?: string }> }) {
  const state = status?.state || "not_configured";
  const StateIcon = statusIcon(state);
  return <div className="min-w-0 border-r border-border/60 p-4 last:border-r-0"><div className="flex items-center justify-between gap-2"><span className="flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.16em] text-muted-foreground"><Icon className="size-3.5" />{name}</span><StateIcon className={`size-4 ${statusClass(state)}`} /></div><p className={`mt-3 truncate font-display text-lg font-bold capitalize ${statusClass(state)}`}>{statusLabel(state)}</p><p className="mt-1 font-mono text-[10px] text-muted-foreground">{status?.latencyMs != null ? `${status.latencyMs}ms probe` : "awaiting probe"}</p></div>;
}

function VitalCard({ item }: { item: WebVitalsSummaryItem }) {
  const config = metrics[item.metric];
  if (!config) return null;
  const rating = ratingFor(item.metric, item.p75);
  return <div className="rounded-2xl border border-border/70 bg-background/45 p-4"><div className="flex items-center justify-between"><span className="font-mono text-xs font-bold tracking-[0.2em] text-muted-foreground">{config.label}</span><Badge variant="outline" className={rating === "good" ? "border-emerald-500/30 text-emerald-600 dark:text-emerald-300" : rating === "poor" ? "border-red-500/30 text-red-600 dark:text-red-300" : "border-amber-500/30 text-amber-600 dark:text-amber-300"}>{rating}</Badge></div><p className="mt-3 font-display text-2xl font-bold tabular-nums">{formatMetric(item.metric, item.p75)}</p><p className="mt-1 text-xs text-muted-foreground">p75 · {item.count} samples</p><div className="mt-3 h-1 rounded-full bg-muted"><div className={`h-full rounded-full ${rating === "good" ? "bg-emerald-500" : rating === "poor" ? "bg-red-500" : "bg-amber-500"}`} style={{ width: `${Math.min(100, item.goodRate || 0)}%` }} /></div></div>;
}

function ServiceFabric({ bytesIn, bytesOut, sampleKey, sampleInterval }: { bytesIn: number; bytesOut: number; sampleKey: string; sampleInterval: number }) {
  // Particles are visual buckets, not fabricated packets: each represents up to
  // 4 KB from the latest measured application bandwidth sample.
  const particleCount = (bytes: number) => bytes > 0 ? Math.min(10, Math.max(1, Math.ceil(bytes / 4096))) : 0;
  const speed = (bytes: number) => `${Math.max(1.4, Math.min(3.8, 3.8 - Math.log10(bytes / 1024 + 1) * 0.7))}s`;
  const opacity = (bytes: number) => bytes > 0 ? Math.min(1, 0.45 + Math.log10(bytes / 1024 + 1) / 3) : 0.16;
  const routes = [
    { id: "railway", label: "RAILWAY", x: 300, y: 20, path: "M300 120L300 20", reversePath: "M300 20L300 120" },
    { id: "vercel", label: "VERCEL", x: 86, y: 55, path: "M300 120L86 55", reversePath: "M86 55L300 120" },
    { id: "mongo", label: "MONGODB", x: 86, y: 205, path: "M300 120L86 205", reversePath: "M86 205L300 120" },
    { id: "s3", label: "S3", x: 514, y: 205, path: "M300 120L514 205", reversePath: "M514 205L300 120" },
    { id: "redis", label: "REDIS", x: 514, y: 55, path: "M300 120L514 55", reversePath: "M514 55L300 120" },
  ];
  const particles = (bytes: number, color: string, path: string, direction: string) => Array.from({ length: particleCount(bytes) }).map((_, index) => <circle key={`${sampleKey}-${direction}-${path}-${index}`} className="transfer-particle" r="2.8" fill={color} opacity={opacity(bytes)}><animateMotion dur={speed(bytes)} begin={`${index * -0.65}s`} repeatCount="indefinite" path={path} /></circle>);
  return <div className="relative flex h-48 items-center justify-center overflow-hidden rounded-2xl border border-border/60 bg-background/25 p-2"><svg viewBox="0 0 600 240" className="absolute inset-0 h-full w-full" role="img" aria-label={`Measured data transfer: ${formatBytes(bytesOut)} sent and ${formatBytes(bytesIn)} received in the latest ${sampleInterval} second sample`}><g fill="none" stroke="hsl(var(--primary) / .22)" strokeWidth="1">{routes.map(route => <path key={route.id} d={route.path} />)}</g><g>{routes.map(route => <g key={`${sampleKey}-${route.id}-traffic`}>{particles(bytesOut, "hsl(var(--primary))", route.path, "out")}{particles(bytesIn, "#10b981", route.reversePath, "in")}</g>)}</g><g fill="hsl(var(--card))" stroke="hsl(var(--primary) / .65)" strokeWidth="1.5">{routes.map(route => <circle key={`${route.id}-node`} cx={route.x} cy={route.y} r="5" />)}<circle cx="300" cy="120" r="18" fill="hsl(var(--primary) / .12)" /></g></svg><div className="relative z-10 rounded-xl border border-primary/30 bg-primary/10 px-5 py-3 text-center"><p className="font-mono text-[9px] uppercase tracking-widest text-primary">API core</p><p className="mt-1 font-display text-lg font-bold">Operational</p></div>{routes.map(route => <span key={`${route.id}-label`} className="absolute rounded-lg border border-border/70 bg-card/80 px-2 py-1 font-mono text-[8px] tracking-wider text-muted-foreground" style={{ left: `${(route.x / 600) * 100}%`, top: `${(route.y / 240) * 100}%`, transform: "translate(-50%, -50%)" }}>{route.label}</span>)}<div className="absolute bottom-2 left-1/2 z-10 flex -translate-x-1/2 gap-3 whitespace-nowrap rounded-full border border-border/70 bg-card/80 px-3 py-1 font-mono text-[9px] uppercase tracking-wider text-muted-foreground"><span className="text-primary">● out {formatBytes(bytesOut)}</span><span className="text-emerald-500">● in {formatBytes(bytesIn)}</span></div></div>;
}

function ReliableServiceFabric({ bytesIn, bytesOut, sampleInterval }: { bytesIn: number; bytesOut: number; sampleInterval: number }) {
  const particleCount = (bytes: number) => bytes > 0 ? Math.min(10, Math.max(1, Math.ceil(bytes / 4096))) : 0;
  const speed = (bytes: number) => `${Math.max(1.2, Math.min(3.5, 3.5 - Math.log10(bytes / 1024 + 1) * 0.55))}s`;
  const routes = [
    { id: "railway", label: "RAILWAY", x: 300, y: 20 }, { id: "vercel", label: "VERCEL", x: 86, y: 55 },
    { id: "mongo", label: "MONGODB", x: 86, y: 205 }, { id: "s3", label: "S3", x: 514, y: 205 }, { id: "redis", label: "REDIS", x: 514, y: 55 },
  ];
  const particleBurst = (bytes: number, color: string, start: { x: number; y: number }, end: { x: number; y: number }, direction: string) => Array.from({ length: particleCount(bytes) }).map((_, index) => <circle key={`${direction}-${start.x}-${end.x}-${index}`} cx={start.x} cy={start.y} r="2.8" fill={color} className="transfer-particle"><animate attributeName="cx" from={start.x} to={end.x} dur={speed(bytes)} begin={`${index * 0.18}s`} repeatCount="indefinite" /><animate attributeName="cy" from={start.y} to={end.y} dur={speed(bytes)} begin={`${index * 0.18}s`} repeatCount="indefinite" /></circle>);
  return <div className="relative flex h-48 items-center justify-center overflow-hidden rounded-2xl border border-border/60 bg-background/25 p-2"><svg viewBox="0 0 600 240" className="absolute inset-0 h-full w-full" role="img" aria-label={`Live measured transfer: ${formatBytes(bytesOut)} sent and ${formatBytes(bytesIn)} received in ${sampleInterval} seconds`}><g fill="none" stroke="hsl(var(--primary) / .24)" strokeWidth="1">{routes.map(route => <line key={route.id} x1="300" y1="120" x2={route.x} y2={route.y} />)}</g><g>{routes.map(route => <g key={route.id}>{particleBurst(bytesOut, "hsl(var(--primary))", { x: 300, y: 120 }, route, `${route.id}-out`)}{particleBurst(bytesIn, "#10b981", route, { x: 300, y: 120 }, `${route.id}-in`)}</g>)}</g><g fill="hsl(var(--card))" stroke="hsl(var(--primary) / .7)" strokeWidth="1.5">{routes.map(route => <circle key={`${route.id}-node`} cx={route.x} cy={route.y} r="5" />)}<circle cx="300" cy="120" r="18" fill="hsl(var(--primary) / .12)" /></g></svg><div className="relative z-10 rounded-xl border border-primary/30 bg-primary/10 px-5 py-3 text-center"><p className="font-mono text-[9px] uppercase tracking-widest text-primary">API core</p><p className="mt-1 font-display text-lg font-bold">Operational</p></div>{routes.map(route => <span key={`${route.id}-label`} className="absolute rounded-lg border border-border/70 bg-card/80 px-2 py-1 font-mono text-[8px] tracking-wider text-muted-foreground" style={{ left: `${(route.x / 600) * 100}%`, top: `${(route.y / 240) * 100}%`, transform: "translate(-50%, -50%)" }}>{route.label}</span>)}<div className="absolute bottom-2 left-1/2 z-10 flex -translate-x-1/2 gap-3 whitespace-nowrap rounded-full border border-border/70 bg-card/80 px-3 py-1 font-mono text-[9px] uppercase tracking-wider text-muted-foreground"><span className="text-primary">● out {formatBytes(bytesOut)}</span><span className="text-emerald-500">● in {formatBytes(bytesIn)}</span></div></div>;
}

export default function MonitoringPage() {
  const [days, setDays] = useState<WebVitalsDays>(30);
  const [selectedMetric, setSelectedMetric] = useState("lcp");
  const [now, setNow] = useState(new Date());
  const ops = useOpsOverview();
  const vitals = useWebVitalsStats(days);
  useEffect(() => { const timer = setInterval(() => setNow(new Date()), 1000); return () => clearInterval(timer); }, []);
  const overview = ops.data;
  const vitalData = vitals.data;
  const request = overview?.telemetry.requests;
  const process = overview?.telemetry.process;
  const latestTraffic = overview?.network.bandwidth.at(-1) || { timestamp: "none", bytesIn: 0, bytesOut: 0 };
  const trend = useMemo(() => (vitalData?.trend || []).map(row => ({ date: row.date, value: (row as any)[selectedMetric] })), [vitalData, selectedMetric]);
  const bandwidth = (overview?.network.bandwidth || []).slice(-30).map(row => ({ time: new Date(row.timestamp).toLocaleTimeString([], { hour: "2-digit", minute: "2-digit" }), outbound: Math.round(row.bytesOut / 1024), inbound: Math.round(row.bytesIn / 1024) }));

  return <PageContainer><main className="relative w-full min-w-0 overflow-hidden rounded-[28px] border border-border/70 bg-background/55 shadow-xl backdrop-blur-sm"><div aria-hidden className="pointer-events-none absolute inset-0 opacity-[0.13] [background-image:linear-gradient(to_right,hsl(var(--border))_1px,transparent_1px),linear-gradient(to_bottom,hsl(var(--border))_1px,transparent_1px)] [background-size:34px_34px] [mask-image:linear-gradient(to_bottom,black,transparent_60%)]" />
    <header className="relative border-b border-border/70 p-5 sm:p-7 lg:p-9"><div className="flex flex-col gap-6 lg:flex-row lg:items-end lg:justify-between"><div><div className="flex items-center gap-2 font-mono text-[10px] uppercase tracking-[0.24em] text-muted-foreground"><span className="size-2 rounded-full bg-emerald-500 shadow-[0_0_10px_rgba(16,185,129,.8)]" />Live observability / production</div><p className="mt-6 text-xs font-semibold uppercase tracking-[0.28em] text-primary">System telemetry</p><h1 className="mt-2 font-display text-4xl font-bold tracking-[-0.04em] sm:text-5xl">Monitoring command center</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-muted-foreground">Real-time application, infrastructure and customer experience signals from this deployment.</p></div><div className="flex flex-wrap items-center gap-2"><span className="rounded-xl border border-border/70 bg-card/55 px-3 py-2 font-mono text-xs tabular-nums text-muted-foreground">{now.toLocaleTimeString([], { hour12: false })}</span><Select value={String(days)} onValueChange={value => setDays(Number(value) as WebVitalsDays)}><SelectTrigger className="w-28 bg-background/60" aria-label="Monitoring time range"><SelectValue /></SelectTrigger><SelectContent>{dayOptions.map(day => <SelectItem key={day} value={String(day)}>{day} days</SelectItem>)}</SelectContent></Select><Button variant="outline" size="icon" onClick={() => { void ops.refetch(); void vitals.refetch(); }} disabled={ops.isFetching || vitals.isFetching} title="Refresh"><RefreshCw className={`size-4 ${(ops.isFetching || vitals.isFetching) ? "animate-spin" : ""}`} /></Button></div></div></header>
    {ops.isPending ? <div className="grid gap-px bg-border/60 sm:grid-cols-2 lg:grid-cols-5">{[1, 2, 3, 4, 5].map(item => <Skeleton key={item} className="h-28 rounded-none" />)}</div> : ops.isError ? <div className="relative m-5 flex items-center justify-between rounded-2xl border border-amber-500/30 bg-amber-500/10 p-4 text-sm text-amber-700 dark:text-amber-300"><span>Live telemetry is unavailable. Retry to reconnect.</span><Button size="sm" variant="outline" onClick={() => void ops.refetch()}>Retry</Button></div> : <><section className="relative grid gap-px border-b border-border/70 bg-border/60 sm:grid-cols-2 lg:grid-cols-5"><DependencyCard name="Vercel" status={overview?.dependencies.vercel} icon={Globe2} /><DependencyCard name="Railway" status={overview?.dependencies.railway} icon={Server} /><DependencyCard name="MongoDB" status={overview?.dependencies.mongo} icon={Database} /><DependencyCard name="Redis" status={overview?.dependencies.redis} icon={Zap} /><DependencyCard name="AWS S3" status={overview?.dependencies.s3} icon={HardDrive} /></section>
      <div className="relative space-y-5 p-4 sm:p-6 lg:p-8"><section className="grid gap-5 lg:grid-cols-[1.35fr_.65fr]"><GlassCard><CardHeader><div className="flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-primary">Signal map / measured traffic</p><CardTitle className="mt-2 font-display text-xl">Service fabric</CardTitle></div><Badge variant="outline" className="gap-1.5 border-primary/30 text-primary"><Activity className="size-3" />Live sample</Badge></div></CardHeader><CardContent><ReliableServiceFabric bytesIn={latestTraffic.bytesIn} bytesOut={latestTraffic.bytesOut} sampleInterval={overview?.network.sampleIntervalSeconds || 5} /></CardContent></GlassCard><GlassCard><CardHeader><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-primary">Request pulse</p><CardTitle className="mt-2 font-display text-xl">Traffic health</CardTitle></CardHeader><CardContent><p className="font-display text-5xl font-bold tabular-nums">{request?.requestsPerMinute?.toFixed(1) || "0.0"}<span className="ml-2 text-base font-medium text-muted-foreground">req/min</span></p><div className="mt-6 grid grid-cols-2 gap-3 text-xs"><div className="rounded-xl bg-muted/50 p-3"><p className="text-muted-foreground">p95 latency</p><p className="mt-1 font-mono text-lg font-bold">{request?.latencyMs.p95 || 0}ms</p></div><div className="rounded-xl bg-muted/50 p-3"><p className="text-muted-foreground">5xx errors</p><p className={`mt-1 font-mono text-lg font-bold ${request?.serverErrors ? "text-red-500" : "text-emerald-500"}`}>{request?.serverErrors || 0}</p></div></div></CardContent></GlassCard></section>
      <section className="grid gap-5 xl:grid-cols-[1.2fr_.8fr]"><GlassCard><CardHeader className="flex flex-row items-start justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-primary">Runtime / five minute window</p><CardTitle className="mt-2 font-display text-xl">Infrastructure telemetry</CardTitle></div><Activity className="size-5 text-primary" /></CardHeader><CardContent><div className="grid grid-cols-2 gap-3 sm:grid-cols-4"><div><p className="text-xs text-muted-foreground">CPU</p><p className="mt-1 font-display text-2xl font-bold">{process?.cpuPercent || 0}%</p></div><div><p className="text-xs text-muted-foreground">RSS memory</p><p className="mt-1 font-display text-2xl font-bold">{formatBytes(process?.memoryBytes.rss)}</p></div><div><p className="text-xs text-muted-foreground">Event lag p95</p><p className="mt-1 font-display text-2xl font-bold">{process?.eventLoopLagMs.p95 || 0}<span className="text-sm">ms</span></p></div><div><p className="text-xs text-muted-foreground">Process age</p><p className="mt-1 font-display text-2xl font-bold">{formatUptime(process?.uptimeSeconds)}</p></div></div><div className="mt-6 h-40"><ResponsiveContainer width="100%" height="100%"><AreaChart data={bandwidth}><defs><linearGradient id="bandwidthFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="hsl(var(--primary))" stopOpacity=".28" /><stop offset="100%" stopColor="hsl(var(--primary))" stopOpacity="0" /></linearGradient></defs><CartesianGrid stroke="hsl(var(--border))" strokeDasharray="3 3" /><XAxis dataKey="time" hide /><YAxis hide /><Tooltip contentStyle={{ background: "hsl(var(--card))", border: "1px solid hsl(var(--border))", borderRadius: 12 }} /><Area type="monotone" dataKey="outbound" stroke="hsl(var(--primary))" fill="url(#bandwidthFill)" strokeWidth={2} name="KB out" /><Area type="monotone" dataKey="inbound" stroke="#10b981" fill="none" strokeWidth={1.5} name="KB in" /></AreaChart></ResponsiveContainer></div><div className="flex flex-wrap items-center justify-between gap-2 font-mono text-[10px] uppercase tracking-wider text-muted-foreground"><span className="flex items-center gap-2"><Wifi className="size-3" />Application bandwidth</span><span>{formatBytes(latestTraffic.bytesOut)} out · {formatBytes(latestTraffic.bytesIn)} in</span></div></CardContent></GlassCard><GlassCard><CardHeader><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-primary">Runtime traces</p><CardTitle className="mt-2 font-display text-xl">Recent activity</CardTitle></CardHeader><CardContent><div className="space-y-3">{(overview?.telemetry.recentTraces || []).slice(0, 6).map(trace => <div key={`${trace.timestamp}-${trace.requestId}`} className="flex items-center gap-3 border-b border-border/60 pb-3 last:border-0"><span className={`size-2 shrink-0 rounded-full ${trace.status >= 500 ? "bg-red-500" : trace.status >= 400 ? "bg-amber-500" : "bg-emerald-500"}`} /><div className="min-w-0 flex-1"><p className="truncate font-mono text-xs">{trace.method} {trace.route}</p><p className="mt-1 font-mono text-[10px] text-muted-foreground">{trace.status} · {trace.durationMs}ms · {new Date(trace.timestamp).toLocaleTimeString()}</p></div></div>)}{!overview?.telemetry.recentTraces.length && <p className="py-8 text-center text-sm text-muted-foreground">No request traces yet.</p>}</div></CardContent></GlassCard></section>
      <section><div className="mb-3 flex items-center justify-between"><div><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-primary">Customer experience</p><h2 className="mt-1 font-display text-xl font-bold">Web vitals</h2></div><span className="font-mono text-[10px] text-muted-foreground">{vitalData?.totalSamples || 0} samples / {days} days</span></div>{vitals.isPending ? <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{[1, 2, 3, 4, 5].map(item => <Skeleton key={item} className="h-36 rounded-2xl" />)}</div> : vitalData && vitalData.totalSamples > 0 ? <><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{vitalData.summary.map(item => <VitalCard key={item.metric} item={item} />)}</div><GlassCard className="mt-4"><CardHeader className="flex flex-row flex-wrap items-center justify-between gap-3"><div><CardTitle className="font-display">{metrics[selectedMetric]?.label || selectedMetric.toUpperCase()} trend</CardTitle><p className="mt-1 text-xs text-muted-foreground">Daily p75 performance</p></div><div className="flex flex-wrap gap-1">{Object.keys(metrics).map(metric => <button key={metric} onClick={() => setSelectedMetric(metric)} className={`rounded-lg px-3 py-1.5 font-mono text-[10px] uppercase tracking-wider ${selectedMetric === metric ? "bg-primary text-primary-foreground" : "bg-muted text-muted-foreground hover:text-foreground"}`}>{metrics[metric].label}</button>)}</div></CardHeader><CardContent><div className="h-56"><ResponsiveContainer width="100%" height="100%"><AreaChart data={trend}><CartesianGrid stroke="hsl(var(--border))" strokeDasharray="3 3" /><XAxis dataKey="date" tickLine={false} axisLine={false} fontSize={10} /><YAxis tickLine={false} axisLine={false} fontSize={10} /><Tooltip contentStyle={{ background: "hsl(var(--card))", border: "1px solid hsl(var(--border))", borderRadius: 12 }} /><Area type="monotone" dataKey="value" stroke="hsl(var(--primary))" fill="url(#bandwidthFill)" strokeWidth={2} connectNulls /></AreaChart></ResponsiveContainer></div></CardContent></GlassCard></> : <GlassCard><CardContent className="p-8 text-center"><GaugeIcon /><p className="mt-3 font-display font-bold">No Web Vitals yet</p><p className="mt-1 text-sm text-muted-foreground">Browse the admin to collect real performance samples.</p></CardContent></GlassCard>}</section>
      <GlassCard><CardHeader><CardTitle className="flex items-center gap-2 font-display"><Monitor className="size-4 text-primary" />Route activity</CardTitle><p className="text-xs text-muted-foreground">Sample volume by route. p75 is intentionally omitted because routes contain mixed metric units.</p></CardHeader><CardContent><div className="overflow-x-auto"><table className="w-full min-w-[520px] text-sm"><thead><tr className="border-b border-border/70 text-left font-mono text-[10px] uppercase tracking-wider text-muted-foreground"><th className="pb-3">Route</th><th className="pb-3 text-right">Samples</th><th className="pb-3 text-right">Metric note</th></tr></thead><tbody>{(vitalData?.topRoutes || []).map(route => <tr key={route.route} className="border-b border-border/50 last:border-0"><td className="py-3 font-mono text-xs">{route.route}</td><td className="py-3 text-right tabular-nums">{route.count}</td><td className="py-3 text-right font-mono text-xs text-muted-foreground">mixed units</td></tr>)}</tbody></table></div>{!vitalData?.topRoutes.length && <p className="py-6 text-center text-sm text-muted-foreground">No route data in this range.</p>}</CardContent></GlassCard></div></>}
    <footer className="relative flex flex-col gap-2 border-t border-border/70 px-5 py-4 font-mono text-[10px] uppercase tracking-[0.16em] text-muted-foreground sm:flex-row sm:items-center sm:justify-between"><span>Application-level telemetry / provider-aware status</span><span>Last snapshot: {overview?.generatedAt ? new Date(overview.generatedAt).toLocaleTimeString() : "pending"}</span></footer>
  </main></PageContainer>;
}

function GaugeIcon() { return <Gauge className="mx-auto size-10 text-muted-foreground/40" />; }
